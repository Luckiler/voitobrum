#define SDL_MAIN_HANDLED

#include <SDL2/SDL.h>
#include <assert.h>
#include <cstdio>
#include "V_window.h"

const Uint32 SCREEN_WIDTH = 640;
const Uint32 SCREEN_HEIGHT = 480;

int main(int argc, char *argv[]) {
    V_window *testWindow = new V_window();
    new Thre
    return 0;
}
